generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model tbl_filme {
  id             Int    @id @default(autoincrement())
  nome           String
  sinopse        String
  duracao        Int
  ano_lancamento Int
}
